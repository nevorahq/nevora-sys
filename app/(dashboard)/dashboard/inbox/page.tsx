import { InboxPage } from "@/modules/planner";

/**
 * /dashboard/inbox — Capture Inbox.
 *
 * Thin composition layer: all orchestration lives in modules/planner.
 */
export default function InboxRoute() {
  return <InboxPage />;
}
